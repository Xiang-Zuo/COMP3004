using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShieldCard : Card {
    public ShieldCard(string aName, Kind akind)
    {
        setName(aName);
        setKind(akind);
    }
}
